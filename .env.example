# PostgreSQL
POSTGRES_PASSWORD=cambiar_en_produccion

# Backend (server/.env)
DATABASE_URL=postgresql://postgres:POSTGRES_PASSWORD@localhost:5432/dashboard_restaurante
JWT_SECRET=cambiar_por_clave_larga_y_aleatoria_en_produccion
ADMIN_CODE=codigo_admin_seguro
PORT=9000

# Redis (opcional — si no se define, usa caché en memoria)
REDIS_URL=redis://localhost:6379

# Email alertas stock bajo (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tucorreo@gmail.com
SMTP_PASS=app-password-de-gmail
SMTP_FROM=Dashboard Restaurante <tucorreo@gmail.com>
ALERT_EMAIL=admin@mirestaurante.com
